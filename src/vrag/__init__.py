"""vault-rag-eval: eval-first hybrid retrieval over a synthetic corpus.

A sanitized public mirror of a private retrieval layer. Every committed data
artifact derives from the synthetic ``corpus/`` and is regenerable by
``make fixtures``; CI asserts it. No personal data, ever.
"""

__version__ = "0.1.0"
